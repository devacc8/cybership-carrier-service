export enum CarrierCode {
  UPS = 'UPS',
  FEDEX = 'FEDEX',
  USPS = 'USPS',
  DHL = 'DHL',
}

export enum WeightUnit {
  LBS = 'LBS',
  KGS = 'KGS',
}

export enum DimensionUnit {
  IN = 'IN',
  CM = 'CM',
}
